// const ClientUseCase = require('./usecases/clients.usecase');

// exports.createClientController = async(req, res) =>{
//   try {
//     const result = await ClientUseCase.createClient(req.body);

//     if(result.error) {
//         res.send('No se creo')
//     } if (result.felicitaciones) {
//         //Que haga lo que haría normalmente, cargar redireccionae a una pagina
//     }

//   } catch (error) {
//     console.log(error)
//     }
// }